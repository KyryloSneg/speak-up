model User {
  id            String @id @default(auto()) @map("_id") @db.ObjectId
  username      String @unique
  nickname      String
  picture       String
  letterPicture String
  password      String
  token         Token?
}
